@{
    ViewData["Title"] = "Home Page";
}

<style>
   @@keyframes gradientBackground {
        0%, 100%

    {
        background-position: 0% 50%;
    }

    50% {
        background-position: 100% 50%;
    }

    }

    /* .animated-gradient { */
    /*     background: linear-gradient(-45deg, #a78bfa, #c084fc, #e9d5ff, #d8b4fe); */
    /*     background-size: 300% 300%; */
    /*     animation: gradientBackground 12s ease infinite; */
    /* } */
</style>

<div class="flex flex-col items-center justify-center min-h-screen text-center animated-gradient">
    <h2 class="text-4xl font-bold text-white mb-4 drop-shadow-lg">Welcome!</h2>
    <p>
        <a href="@Url.Action("Index", "TodoItems")"
           class="inline-block bg-white text-purple-700 font-semibold px-6 py-3 rounded-lg shadow-md hover:bg-purple-100 transition">
            View Todo Items
        </a>
    </p>
</div>
